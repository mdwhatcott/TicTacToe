(ns gui.configure-players
  (:require [quil.core :as q]
            [gui.common :as c]))

(defn update [state]
  state)

(defn calculate-anchors [screen-width]
  (let []
    {}))

(defn draw [state]
  (let []

    ))
